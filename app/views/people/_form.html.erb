


<%= form_for :people ,url: people_path(@people) do |f| %>

    <% if @people.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@people.errors.count, "error") %> prohibited
            this article from being saved:
          </h2>
          <ul>
            <% @people.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </p>

    <p>
      <%= f.label :text %><br>
      <%= f.text_area :text %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>